#define         RL 1
float Analog_value = 0;
float VRL=0;
float Rs=0;
float Ro=0;
#define         MQ2 A0
void setup() {
  // put your setup code here, to run once:
//pinMode(MQ2,INPUT);
Serial.begin(115200);

}

void loop() 
{
 Analog_value=analogRead(MQ2);

 VRL =  Analog_value*(5.0/1023);
 Rs=((5.0/VRL)-1)*RL;
 Ro=Rs/9.8;

 Serial.print("Ro= ");
 Serial.println(Ro);
  Serial.print("VRL= ");
 Serial.println(VRL);
 
 delay(1000);
}